# BBIOS OS Service Contracts

## 1. Document Control

- Document: Service Contracts
- Phase: 2E Service Layer Contract Standardization
- Branch: `phase-2/repository-contract-cleanup`
- Baseline before Phase 2E: commit `2406604`, 113 tests passing
- Scope: service-layer contracts, dependency clarity, and compatibility behavior
- Out of scope: PostgreSQL, SQLAlchemy, Alembic, authentication redesign, deployment, route changes, response-envelope changes, repository format changes, DTO migration, and unit-of-work implementation

## 2. Contract Principles

- Services receive repositories, engines, routers, dashboards, controls, and other collaborators explicitly where practical.
- Services must call public repository methods only.
- Services must not read or write JSON files or filesystem paths directly.
- Services own business rules and orchestration; handlers and FastAPI adapters own transport translation.
- Existing return values, exception types, route behavior, response envelopes, compatibility facades, and module-level lifecycle behavior remain unchanged unless separately approved.
- Public service methods should document their current inputs, outputs, and side effects when behavior is not obvious from the method name.

## 3. Service Inventory

| Component | Module | Responsibility | Constructor Dependencies | Repository Use | Other Services | Public Methods | Inputs | Returns | Missing-Record Behavior | Exceptions | Compatibility | Direct Persistence | Module-Level Instance | Tests | Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TaskService` | `bbi_os/task_management/service.py` | Task CRUD business rules | `JsonTaskRepository` | Public `save`, `list`, `get`, `delete` | None | `create`, `list`, `get`, `update`, `delete` | Dict task payloads, task id strings | Task dictionaries or `None` for delete | Raises `TaskNotFoundError` through `_find` or delete | `ValidationError`, `TaskNotFoundError` | Dictionary task contract retained | No | No | `tests/test_task_service.py`, `tests/test_task_api.py`, adapter tests | Low |
| `ClientManagementService` | `bbi_os/cockpit/client_management.py` | Client creation/listing and plan assignment | `EntityRepository`, `ClientPlanRegistry` | Public `save`, `list`, `delete`; plan `assign`, `plan_for` | None | `create`, `list` | Dict client payloads | Client dictionaries | Not exposed by public `get`; create rolls back entity on plan failure | `InvalidClient`, propagated persistence errors | `/clients` and `/v1/clients` handler contract retained | No | No | `bbi_os/cockpit/tests/test_client_management.py`, adapter tests | Low |
| `ClientExecutionService` | `bbi_os/client_execution/service.py` | Client execution orchestration and access checks | `EntityRepository`, `ClientExecutionRouter`, `ClientExecutionEngine`, `ExecutionStateRepository` | Public client `get`; state `latest_for_client`, `get` through `get_execution` | `ClientExecutionEngine` | `start`, `schedule`, `status`, `get_execution` | Dict execution payloads, client id, execution id | `ClientExecutionRecord` or `None` from `get_execution` | `status` raises `ExecutionNotFound`; `get_execution` returns `None` | Execution auth/client/request errors | Existing status behavior retained | No | No | `bbi_os/client_execution/tests/test_execution.py`, `tests/test_service_contracts.py` | Low |
| `ClientExecutionEngine` | `bbi_os/client_execution/engine.py` | Schedule/execute records and transition state | `ClientWorkflowExecutor`, `ExecutionStateRepository` | Public `save` through state machine/repository | `ClientWorkflowExecutor`, `ExecutionStateMachine` | `schedule`, `execute` | `ClientExecutionRequest` | `ClientExecutionRecord` | Not applicable | `ConcurrentExecution`; workflow failures recorded as failed records | Current compensation behavior retained | No | No | execution tests | Medium |
| `WorkflowEngine` | `bbi_os/workflows/engine.py` | Workflow definition creation, triggering, retry, rollback | `WorkflowRepository`, `WorkflowActionRegistry` | Public workflow definition/instance methods | Action handlers | `create_definition`, `trigger`, `retry`, `get_status`, `get_history` | Dict workflow definitions, ids, input dicts | `WorkflowDefinition`, `WorkflowInstance`, step history list | Raises `WorkflowNotFound` | Workflow definition/execution/step errors | Handler and template behavior retained | No | No | `tests/test_workflows.py`, integration/template tests | Medium |
| `WorkflowTemplateService` | `bbi_os/workflows/templates.py` | Template validation, lookup, binding, execution lineage | `WorkflowTemplateRepository`, `WorkflowEngine` | Public template and lineage methods | `WorkflowEngine` | `create`, `list`, `get`, `execute` | Template dicts, references, parameters, input dicts | `WorkflowTemplate`, `(WorkflowInstance, lineage dict)` | Repository raises template not found errors | `InvalidWorkflowTemplate`, `TemplateNotFound`, `TemplateVersionNotFound` | Existing template execution contract retained | No | No | `tests/test_workflow_templates.py` | Medium |
| `OnboardingService` | `bbi_os/client_onboarding/service.py` | Authenticated onboarding orchestration | `OnboardingRouter`, `OnboardingEngine` | None directly | `OnboardingEngine` | `onboard` | Dict onboarding payloads | `OnboardingResult` | Router/template failures are propagated as onboarding errors | `OnboardingAuthenticationFailed`, `OnboardingError` subclasses | Handler route behavior retained | No | No | onboarding tests | Low |
| `OnboardingEngine` | `bbi_os/client_onboarding/onboarding_engine.py` | Execute routed onboarding template | `WorkflowTemplateService` | None directly | `WorkflowTemplateService` | `execute` | Request id, `OnboardingRequest`, `OnboardingRoute` | `OnboardingResult` | Template not found mapped to `OnboardingWorkflowNotFound` | `OnboardingExecutionFailed` | Existing workflow-backed onboarding retained | No | No | onboarding tests | Medium |
| `OnboardingWorkflowActions` | `bbi_os/client_onboarding/onboarding_engine.py` | Workflow action adapter for onboarding entity operations | `EntityRepositoryRouter`, `OutboundRequestEngine` | Public `repository_for`, `save`, `get`, `delete` | `OutboundRequestEngine` | `execute`, `rollback` | Action input/rollback dictionaries | `ActionResult` or `None` | Missing client entity raises `InvalidOnboardingRequest` | `InvalidOnboardingRequest` | Workflow action handler contract retained | No | No | onboarding tests | Medium |
| `ClientPipelineService` | `bbi_os/client_pipeline/service.py` | Authenticated client request pipeline orchestration | `ClientRequestRouter`, `ClientPipelineEngine` | None | `ClientPipelineEngine` | `process` | Dict request payloads | `ClientPipelineResult` | Router/template failures are propagated as pipeline errors | `PipelineAuthenticationFailed`, `ClientPipelineError` subclasses | Handler route behavior retained | No | No | pipeline tests | Low |
| `ClientPipelineEngine` | `bbi_os/client_pipeline/pipeline_engine.py` | Execute routed workflow template for client requests | `WorkflowTemplateService` | None | `WorkflowTemplateService` | `execute` | `ClientRequest`, `TemplateRoute` | `ClientPipelineResult` | Template not found mapped to `PipelineWorkflowNotFound` | `PipelineExecutionFailed` | Existing template-backed pipeline retained | No | No | pipeline tests | Medium |
| `ClientMonetizationService` | `bbi_os/client_monetization/service.py` | Plan lookup, usage recording, billing metrics | `EntityRepository`, `ClientPlanRegistry`, `UsageTracker`, `PricingEngine`, optional `PlanEnforcer`, optional `BillingSummaryGenerator` | Public client `get`; plan `plan_for`; usage through helpers and `record` | `PricingEngine`, `PlanEnforcer`, `BillingSummaryGenerator` | `get_plan`, `track`, `record_automatic`, `metrics`, `billing_summary` | Client ids and usage dictionaries | Dictionaries or `UsageEvent` | Missing client raises `MonetizationClientNotFound` | Monetization auth/client/limit errors | Default helper construction retained | No | No | monetization tests, `tests/test_service_contracts.py` | Low |
| `PricingEngine` | `bbi_os/client_monetization/pricing_engine.py` | Usage price calculation | None | None | None | `estimate` | `ClientPlan`, event type, units | Float | Not applicable | Key errors if unsupported plan/event reaches engine | Current pricing table retained | No | No | monetization tests | Low |
| `OutboundRequestEngine` | `bbi_os/integrations/outbound.py` | External HTTP execution with validation/retry | `IntegrationRegistry`, optional `HttpTransport`, timeout, retries | Public `get_connector` | Transport protocol | `execute` | Connector/method/path/body/query/version | Normalized response dictionary | Registry raises connector not found | External request/timeout errors | Existing transport injection retained | No file persistence; reads env credentials | No | integration tests | Medium |
| `ConnectorWorkflowAction` | `bbi_os/integrations/outbound.py` | Workflow action adapter for outbound requests | `OutboundRequestEngine` | None | `OutboundRequestEngine` | `execute`, `rollback` | Action input/rollback dictionaries | `ActionResult` or `None` | Connector lookup handled by outbound engine | External request errors | Workflow action contract retained | No | No | integration/execution tests | Low |
| `WebhookService` | `bbi_os/integrations/webhooks.py` | Webhook registration, payload validation, signature check, workflow trigger | `IntegrationRegistry`, `WorkflowEngine` | Public `register_webhook`, `get_webhook` | `WorkflowEngine` | `register`, `invoke` | Webhook dictionaries, webhook id, payload, signature | `WebhookRegistration`, `WorkflowInstance` | Registry raises webhook not found validation error | `WebhookValidationFailed`, workflow errors | Handler behavior retained | No file persistence; reads env secret | No | integration tests | Medium |
| `IntegrationWorkflowEngine` | `bbi_os/integrations/workflow.py` | Workflow engine extension that scopes workflow instance id | Inherits `WorkflowEngine` dependencies | Same as `WorkflowEngine` | Same as `WorkflowEngine` | Inherited public workflow methods | Workflow inputs | Workflow outputs | Same as `WorkflowEngine` | Same as `WorkflowEngine` | Current context behavior retained | No | No | integration/execution tests | Medium |
| `CockpitService` | `bbi_os/cockpit/service.py` | Prototype client facade plus rich cockpit dashboard/control facade | Optional repositories, dashboards, controls, analytics | Public `list`, `list_for_client` where present | Dashboards, controls, analytics | Prototype: `validate_client`, `create_client`, `get_client`, `search_clients`, `test_pipeline`; rich: `system_overview`, `client`, `execution_monitor`, `usage`, `billing`, `workflow_control`, `execute`, `retry`, `cancel` | Prototype strings/dicts; rich ids/dicts | Existing dictionaries or `None` for cancel | Prototype returns error dicts; rich requires dependencies | `RuntimeError`, `CockpitControlError` from collaborators | Zero-argument construction retained | No | `bbi_os/cockpit/router.py` creates module-level service | cockpit tests | High |
| `SystemOverviewDashboard` | `bbi_os/cockpit/dashboards/system_overview.py` | Aggregate cockpit system overview | Clients repository, `SystemHealthEngine`, `CockpitEventStore` | Public client `list` | Health engine/event store | `render` | Execution records | Dictionary | None | Collaborator exceptions | Dashboard contract retained | No | No | cockpit tests | Low |
| `ClientViewDashboard` | `bbi_os/cockpit/dashboards/client_view.py` | Client-specific cockpit view | Clients repository, monetization service, client controls, event store | Public client `get` | Monetization service, controls | `render` | Client id, execution records | Dictionary | Raises `CockpitControlError` for missing client | `CockpitControlError` | Dashboard contract retained | No | No | cockpit tests | Medium |
| `ExecutionMonitorDashboard` | `bbi_os/cockpit/dashboards/execution_monitor.py` | Enrich execution records with workflow history and connector calls | `WorkflowRepository`, `CockpitEventStore` | Public `get_instance` | Event store | `render` | Execution records | Dictionary | Missing workflow instance yields empty history | None explicit | Dashboard contract retained | No | No | cockpit tests | Low |
| `MonetizationDashboard` | `bbi_os/cockpit/dashboards/monetization_view.py` | Aggregate usage and billing dashboard data | `ClientMonetizationService` | None directly | Monetization service | `usage`, `billing` | Client ids | Dictionaries | Missing clients handled by monetization service | Monetization exceptions | Dashboard contract retained | No | No | cockpit tests | Low |
| `WorkflowControlDashboard` | `bbi_os/cockpit/dashboards/workflow_control.py` | Show active workflow records, templates, queue | `WorkflowTemplateService`, `ExecutionStateRepository` | Public through templates; execution repo dependency retained but not read directly | Template service | `render` | Active records | Dictionary | None | Collaborator exceptions | Dashboard contract retained | No | No | cockpit tests | Low |
| `ExecutionControls` | `bbi_os/cockpit/controls/execution_controls.py` | Cockpit command adapter over execution service | `ClientExecutionService` | None directly after Phase 2E | Client execution service | `start`, `retry`, `pause`, `cancel`, `inspect` | Dicts and execution ids | `ClientExecutionRecord` or `None` for unsupported commands | Missing execution raises `CockpitControlError` | `CockpitControlError` | Existing unsupported pause/cancel retained | No | No | cockpit/service-contract tests | Low |
| `ClientControls` | `bbi_os/cockpit/controls/client_controls.py` | In-memory cockpit lock state and execution trigger gate | `ExecutionControls` | None | Execution controls | `trigger`, `lock`, `unlock`, `is_locked` | Dicts and client ids | Execution record, bool, or `None` | Not applicable | `CockpitControlError` when locked | In-memory lock compatibility retained | No | No | cockpit tests | Low |
| `SystemHealthEngine` | `bbi_os/cockpit/analytics/system_health.py` | Calculate health metrics | `CockpitEventStore` | None | Event store | `calculate` | Execution records | Dictionary | Not applicable | None explicit | Analytics contract retained | No | No | cockpit tests | Low |
| `UsageInsightsEngine` | `bbi_os/cockpit/analytics/usage_insights.py` | Calculate usage insight aggregates | `ClientMonetizationService` | None | Monetization service | `calculate` | Client ids, execution records | Dictionary | Missing clients handled by monetization service | Monetization exceptions | Analytics contract retained | No | No | cockpit tests | Low |
| `PerformanceMetricsEngine` | `bbi_os/cockpit/analytics/performance_metrics.py` | Calculate workflow duration/failure metrics | `Observability` | None | Observability | `calculate` | Execution records | Dictionary | Not applicable | Timestamp parse errors if malformed records reach engine | Analytics contract retained | No | No | cockpit tests | Low |

## 4. Phase 2E Implemented Changes

- `ClientExecutionService.get_execution(execution_id)` was added as a public read contract for execution-id lookup. It delegates to `ExecutionStateRepository.get()` and returns the same `ClientExecutionRecord` or `None` value.
- `ExecutionControls.retry()` and `ExecutionControls.inspect()` now call `ClientExecutionService.get_execution()` when available, with the previous `state_repository.get()` path retained only as a compatibility fallback for existing execution-service test doubles.
- `ClientMonetizationService` now accepts optional `PlanEnforcer` and `BillingSummaryGenerator` collaborators while preserving existing default construction.

## 5. Compatibility Retained

- `ClientExecutionService.status(client_id)` still validates client access and raises `ExecutionNotFound` when no latest client execution exists.
- `ExecutionControls.inspect()` and retry lookup still raise `CockpitControlError("Execution was not found")` for missing execution ids.
- Existing execution-service test doubles that expose `state_repository` but not `get_execution()` remain compatible.
- `ExecutionControls.retry()` still restarts failed records through `ClientExecutionService.start()` with the existing payload shape.
- `ClientMonetizationService` four-argument construction still creates `PlanEnforcer(usage)` and `BillingSummaryGenerator(usage)`.
- No route, response envelope, dataclass, dictionary payload, JSON schema, or persistence path changed.

## 6. Deferred Service-Layer Debt

1. `CockpitService` remains a mixed prototype/rich compatibility facade with loose `Any` dependencies and zero-argument construction.
2. Module-level compatibility instances remain in `bbi_os/cockpit/router.py` and generated prototype code; lifecycle consolidation is deferred.
3. Secret and credential reads in integration services still use environment variables directly pending security/configuration governance.
4. Workflow and template services still exchange dictionaries at several boundaries; broad DTO migration is deferred.
5. Shared service composition helpers for runtime adapters are deferred until richer `/v1/*` FastAPI composition is approved.

## 7. Phase 2E Exit Criteria

- Service-like components have been inventoried.
- Services use public repository methods for changed behavior.
- Direct service reach-through to execution repository state has been replaced by a public service read method.
- Monetization helper collaborators can be injected for composition and testing while defaults remain compatible.
- Focused regression tests cover the changed service contracts.
- Full unittest discovery, compileall, and diff checks pass.
